<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- App runtime theme — used after splash dismisses -->
    <style name="Theme.RentLog" parent="Theme.AppCompat.DayNight.NoActionBar" />

    <!--
        Splash screen theme — set on MainActivity in AndroidManifest.
        Uses the Android 12+ SplashScreen API (androidx.core:core-splashscreen).
        postSplashScreenTheme hands off to Theme.RentLog once content is ready.
    -->
    <style name="Theme.RentLog.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_icon_background</item>
        <item name="postSplashScreenTheme">@style/Theme.RentLog</item>
    </style>

</resources>
